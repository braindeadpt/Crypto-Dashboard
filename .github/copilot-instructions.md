Read `AGENTS.md` at the repo root — it is the agent contract for this
project (product rules, commands, boundaries). Skills live in
`.agents/skills/`; `.github/skills` junctions to it on `npm install`.
